server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;  # Adjust this path to where your Vue app is built
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}